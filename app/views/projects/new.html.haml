%h2=t '.title'

%p=t '.explain1_html'
%p=t '.explain2_html'

= form_for @project do |f|
  = project_fields(f,@project)
  .submit
    = link_to t('.cancel'), :back, :type => 'button'
    = f.submit t('.submit'), :'data-disable-with' => t('.sending')

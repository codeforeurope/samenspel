#page_menu
  - if Teambox.config.enable_conversations_module
    = link_to t('shared.project_navigation.conversations'), project_conversations_path(project), :class => controller_selected?('conversations')
  = link_to t('shared.project_navigation.task_lists'), project_task_lists_path(project), :class => controller_selected?(['task_lists', 'tasks'])
  - if Teambox.config.enable_pages_module
    = link_to t('shared.project_navigation.pages'), project_pages_path(project), :class => controller_selected?('pages')
  -# = link_to t('shared.project_navigation.files'), project_uploads_path(project), :class => controller_selected?('uploads')
  = link_to t('shared.project_navigation.people'), project_people_path(project), :class => controller_selected?('people')
  = link_to t('shared.project_navigation.contacts'), project_contacts_path(project), :class => controller_selected?('contacts')
